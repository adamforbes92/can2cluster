
# Can2Cluster
Can2Cluster is designed to take CAN messages and convert them into an analog output for use on clusters with traditional pull-down style inputs.  It is geared towards VAG CAN messages but Ford and Emerald ECUs CAN IDs have been added too.

The code is based for an ESP32 DevKit V1, and will capture incoming CAN messages and convert them into useable 'analog' signals - like RPM, Speed, Engine Management Light (EML), Electronic Pedal Control Light (EPC) and Reverse (from DSG).  A full breakdown is below.  

Alternative setups and CAN IDs can be added and would allow custom features.

DSG gearboxes are supported and speed is currently calculated using the current gear value and RPM.  KWP2000 / UDS has been implemented and awaits testing!

On MQB vehicles, pressing the DSG Paddle +/- inputs is relayed onto the CAN bus as the real shifter/paddle module's own frame (`0x0AF`), reverse-engineered directly from a live shifter capture rather than a generic aftermarket emulation - so it lands on the bus exactly as the factory paddles would.

If no speed is available (from either hall or CAN), an optional GPS module (like the Neo6M) can be used to capture speed via. GPS.  This can be broadcast via. CAN if required - users should know the ID they would like it broadcast to.  A reminder that GPS modules need to see the sky and depending on installation location may need an external aerial to be fitted.  It uses a standard SMA connector.

WiFi calibration is supported to minimise code editting and provides valueable feedback on incoming messages.  It is viewable on 192.168.1.1 and is available for 60 seconds after boot.  If no devices connect, the WiFi will power-down automatically to reduce power consumption and heat generated by the unit.

**There is a difference in pin-out between Version 1 and Version 2 PCBs - although functionality is the same.  Confirm pinouts using the 'Module Pinout' table.**

This can be expanded to support other marques and is actively encouraged.  Other users are actively using the DSG support and using the EML/EPC outputs to configure shift locks and other features.

![Can2Cluster Web UI — dashboard, needle sweep, shift light, speed & RPM sources, status, live inputs, output tests and OTA](/Images/can2clusterUI.png)

![Can2Cluster Web UI — DSG park and speed calculation, custom CAN input, GPS & speed broadcast, CAN analyzer, coolant gauge and board diagnostics](/Images/can2clusterUI-2.png)

![Board Overview](/Images/BoardOverview.png)

### Purchase
If you want to purchase an assembled Can2Cluster, you can do so here: [Can2Cluster - Forbes Automotive](https://forbes-automotive.com/products/can2cluster)

## IO
Can2Cluster uses an 18-pin MX23A18 connector and features the following IO:

#### IO List
| Type | Signal | Notes |
|-----|--------|-------|
| Input | CAN (High & Low) | CANBUS |
| Input | DSG Paddle + | Ground to activate |
| Input | DSG Paddle - | Ground to activate |
| Input | Hall Speed | Pull-up/down options |
| Input | Hall RPM | Pull-down ONLY |
| Input | GPS | Neo6M or similar |
| Output | RPM (MK1/MK2) | High voltage 'coil type' |
| Output | RPM Square | Pull-down ONLY |
| Output | Speed Square | Pull-down ONLY |
| Output | EML & EPC | Pull-down ONLY |
| 11 | Reverse | 5A high-side |

#### Optional / Cool Features 
Shift Light (RPM vs. EML/EPC)
Needle Sweep
Park release for DSG

### Pinout
The MX23A18 connector pinout is:
![Board Overview](/Images/BoardConnector.png)

| Pin/ | Signal | Notes |
|-----|--------|-------|
| 1 | Vign | 12V Ignition |
| 2 | Ground | Chassis ground — |
| 3 | Chassis CAN Low | to Chassis/ECU side |
| 4 | Chassis CAN High | to Chassis/ECU side |
| 5 | Speed | Speed output; pull-down |
| 6 | RPM | RPM output; pull-down |
| 7 | EML | Engine Management Light; pull-down |
| 8 | EPC | Electronic Pedal Control Light; pull-down |
| 9 | Reverse | 12v high-side reverse output; 5A max |
| 10 | Paddle Up | For DSG; pull-down |
| 11 | Paddle Down | For DSG; pull-down |
| 12 | Hall Speed | Optional pull-up/down |
| 13 | N/A | Not used |
| 14 | N/A | Not used |
| 15 | N/A | Not used |
| 16 | N/A | Not used |
| 17 | N/A | Not used |
| 18 | RPM | High voltage coil pulse; MK1/MK2 etc |

> **Legacy clusters & needle jitter:** On some older cluster, the coil-style RPM output can induce a little jitter in the hall inputs due to poor grounding. Fitting a **~500 Ω resistor in series** with the RPM output largely reduces this.

## Setup
Purchased modules will come pre-loaded with the most recent firmware, check back here for updates.  

> Connect the module as per the wiring diagram

> On initial power up, WiFi is available and will remain active while any client is connected. Once the last client disconnects, the radio turns off automatically after 1 minute to save power (see Power Management below).

> Connect to the device and search for '192.168.1.1' in a browser

> Configure the device to suit: Needle Sweep, Sweep Rate, RPM Type, Shift Light

> Use the 'IO' tab to confirm incoming data

### WiFi Setup
Connect to the **`Can2Cluster`** access point (open network, `192.168.1.1`) and browse to **192.168.1.1** or **c2c.local**. The Web UI is built on Forbes Automotive's shared dark theme, the same look used across OpenHaldex, SpeedPulser, SpeedPulserPro, can2rpm, the MQB Steering Wheel Controller and AirLift Controller. Current firmware version: **3.23**.

Multiple options are available in WiFi:
> Needle Sweep (rate adjustable)

> RPM Output Selection

> Shift light outputs (EML/EPC or both)

> Park Lock Release (for DSG)

> Speed Selection (Hall/ECU/ABS/DSG/GPS)


<p align="center">
  <img src="/Images/ui-dashboard.png" alt="Dashboard — RPM and speed gauges, system status pills and display options" width="300">
  &nbsp;&nbsp;
  <img src="/Images/ui-needle-sweep.png" alt="Configuration — needle sweep enable, sweep speed and RPM / speed step" width="300">
</p>
<p align="center">
  <img src="/Images/ui-shift-light.png" alt="Configuration — shift light output, RPM limit and flashes; cluster output coil / MPH options" width="300">
  &nbsp;&nbsp;
  <img src="/Images/ui-dsg-park.png" alt="Configuration — DSG park behaviour (park indicator output)" width="300">
</p>
<p align="center">
  <img src="/Images/ui-speed-rpm.png" alt="Advanced — speed and RPM source selection with hall / VR frequency limits" width="300">
  &nbsp;&nbsp;
  <img src="/Images/ui-dsg-speed.png" alt="Advanced — DSG speed calculation: gear ratios, final drives and tyre circumference" width="300">
</p>

*Dashboard with the final RPM/speed as dial gauges and the status pills; needle sweep, shift light, cluster output and DSG park release on the Configuration tab; speed and RPM source selection under Advanced. Choosing **DSG** as the speed source reveals the gear-ratio calculator (speed = RPM × tyre circumference ÷ gear ratio × final drive).*

**Status indicators:** CAN health, GPS and output (EML/EPC/Reverse) status are shown as coloured pills in the Web UI:

| Colour | Meaning |
|---|---|
| 🟢 Green | Healthy / active — CAN bus is healthy, GPS has a satellite lock, or the output reflects a real vehicle CAN signal |
| 🔴 Red | Unhealthy / not available — CAN bus unhealthy, or no GPS module fitted/connected |
| 🟠 Orange | A caveat, not a failure — GPS module is connected but has no satellite fix yet ("Connected (no sats)"), or an EML/EPC/Reverse output is being **forced on via the built-in test/diagnostic mode** rather than a real vehicle signal |


<p align="center">
  <img src="/Images/ui-system-status.png" alt="Dashboard — system status pills: CAN bus, GPS, EML, EPC, reverse, park, paddles" width="300">
</p>

## Adding CAN Functionality
Users wishing to add CAN functionality not available are encourged to push commits to the project.  The main section of code for managing imcoming CAN messages are handled in an interrupt and corresponding outputs are pushed automatically.  Users should know the CAN ID and Bytes they wish to process and what variables to assign them to.

Two things in the Web UI help here. **CAN Analyzer** (Advanced tab) forwards every received frame to [SavvyCAN](https://www.savvycan.com/) over WiFi (GVRET, `192.168.1.1:23`) or over the USB serial port, so you can find the ID and bytes you're after. And for a speed signal on a non-standard ID, **Custom CAN Input** (choose *Custom* as the speed source) lets you enter the CAN ID, byte positions, byte order, scale and offset without touching the code:

<p align="center">
  <img src="/Images/ui-can-analyzer.png" alt="Advanced — CAN Analyzer: forward frames to SavvyCAN over WiFi or serial" width="300">
  &nbsp;&nbsp;
  <img src="/Images/ui-custom-can.png" alt="Advanced — Custom CAN Input: CAN ID, byte indexes, byte order, scale and offset, with the parsed speed shown live" width="300">
</p>

For example (in _CAN.ino under "onBodyRX");
```
    case emeraldECU1_ID:
      vehicleRPM = ((frame.buf[0] << 8) | frame.buf[1]);  // conversion: 0.25*HEX // this is RPM
      break;

    case emeraldECU2_ID:
      vehicleSpeed = ((frame.buf[2] << 8) | frame.buf[3]) * (2.25 / 256);  // conversion: 0.25*HEX // this is RPM
      break;

```
The above entry will check to see if there is a CAN message from "0x1000" or "0x1001" (Emerald ECU 1 & 2) and then parse the data to the relevant variables.

## Adding GPS
Users wishing to add GPS at a future date can do - the boards are already socketed in readiness.  The 4-pin JST XH Connector will provide 3.3v, RX, TX and ground.

![GPS Connection](/Images/BoardGPS.png)

Once fitted, pick **GPS** as the speed source on the Advanced tab. The same tab sets the receiver's **update rate** and, if another module on the bus needs the speed, **Broadcast Speed** puts it onto CAN on an ID, byte layout and scale of your choosing:

<p align="center">
  <img src="/Images/ui-gps.png" alt="Advanced — Broadcast Speed (CAN ID, DLC, byte layout, scale) and GPS Update Rate" width="300">
</p>

## Hall & RPM Input Parsing
When speed or RPM is taken from the hall/VR/pulse inputs (rather than CAN), the frequency is measured within a **windowed capture** rather than a single edge-to-edge period. Each interrupt accumulates the edge intervals and a count, and the task averages them once per loop (`frequency = intervals ÷ summed interval time`) — which gives a true average over a period.

This matters because a single-period reading turns tone-wheel / tooth-spacing jitter straight into a jumpy needle. Pulses that are arriving closer than ~3 ms (> 333 Hz) are rejected as electrical noise, so electrical or ignition-coil EMI on the input can't be mistaken for a valid speed or RPM. The averaged frequency is then mapped to the cluster scale (`map(frequency, 0, maxFreqHall/maxRPM, 0, maxSpeed/clusterRPMLimit)`).

Every input the module can see is listed side by side on the **Diagnostics** tab, so it is obvious which sources are live before choosing one, and the output tests drive the cluster directly from the page:

<p align="center">
  <img src="/Images/ui-live-inputs.png" alt="Diagnostics — live data from every input: RPM, hall, CAN, VR, ECU, ABS, DSG, TP2.0, UDS, GPS and custom CAN speed" width="300">
  &nbsp;&nbsp;
  <img src="/Images/ui-output-tests.png" alt="Diagnostics — speed and RPM output tests, EML / EPC / reverse output tests and diagnostic mode" width="300">
</p>

*Diagnostics: every speed and RPM source side by side (left); slider-driven speed and RPM output tests, the EML / EPC / reverse output tests and the diagnostic (forced-output) mode that shows as orange pills on the dashboard (right).*

The Diagnostics tab also reports which board revision was detected and whether the I2C peripherals on the V2 PCB (the coolant DAC and the port expander that drives Reverse / EPC / EML and reads the DSG paddles) answered:

<p align="center">
  <img src="/Images/ui-board-diag.png" alt="Diagnostics — Board & I2C diagnostics: board revision, MCP4725 DAC, TCA9554 expander, coolant DAC value" width="300">
</p>

## Coolant Gauge Output
On the V2 PCB the MK2-style coolant temperature gauge can be driven from the CAN coolant temperature through one of the ULN2003 outputs (EML or EPC — selecting one takes that pin away from the shift light / DSG park use). Above the **warning temperature** the gauge is pegged and the cluster warning light is triggered.

Because every gauge is different, the **Calibration Builder** lets you drive the needle directly: enable calibration mode, jog the duty until the needle sits on a known mark, pick that temperature and **Capture**. The stored points are drawn as a curve and interpolated at runtime.

<p align="center">
  <img src="/Images/ui-coolant.png" alt="Advanced — Coolant Gauge Output: output pin, warning temperature, live coolant temperature and applied duty" width="300">
  &nbsp;&nbsp;
  <img src="/Images/ui-coolant-cal.png" alt="Advanced — Calibration Builder with duty jog buttons, captured points and the resulting calibration curve" width="300">
</p>

## Jumpers
To keep functionality, there are a variety of jumpers available on the board which can be added/removed to suit the chassis.

![Jumpers](/Images/Jumpers.png)

### R-Term
The jumper marked 'r-term' is the terminating resistor for the CANBUS network.  If there are no other CAN devices on the network (this is the only one), the jumper should remain.  If there are other devices on the network, this can be removed.

### SpeedPulser
A selector for a hall sensor can be configured as pull-up or pull-down with the central pin being the hall sensor itself.  Typically, VAG ones are 'pull-up'.

### 12v Pullup
Used to configure outputs if the cluster does not have internal pull-ups.

### Over-the-Air Updates (Two-Step)
OTA is enabled, so updating to the latest release is seamless and doesn't require removing the module from the vehicle. Firmware and the web UI live on separate flash partitions, so an update is done in **two steps** from the OTA tab:

1. **Filesystem** — upload `littlefs.bin` (`POST /api/ota/fs`, written to the SPIFFS partition). Updates the web UI (`index.html`, `app.js`, `style.css`). The device does **not** reboot after this step.
2. **Firmware** — upload `firmware.bin` (`POST /api/ota`, written to the OTA app partition). Updates the application and reboots automatically once complete.

`GET /api/ota/info` reports the running version, board and hardware.

> Download the most recent 'Release' - this will be saved locally on your phone/laptop/etc as `.bin` file(s)

> Connect to Can2Cluster on WiFi (or browse to c2c.local)

> Go to 192.168.1.1 and choose the OTA tab

> Upload the filesystem image first, then the firmware image

> Wait for the ESP to reboot after the firmware step


<p align="center">
  <img src="/Images/ui-ota.png" alt="OTA tab — firmware info and two-step update" width="300">
</p>

## Power Management
The firmware includes a universal reduced-power module that automatically saves current once no clients are connected to the WiFi AP. This directly reduces heat generated by the on-board linear voltage regulator — a linear regulator dissipates heat proportional to `(Vin − Vout) × I_load`, so every milliamp saved is less heat.

**What happens 1 minute after the last client disconnects:**
> WiFi radio off — the single biggest saving (~80–120 mA average)

> CPU frequency scaled: 240 MHz → 80 MHz

> Bluetooth controller disabled at boot (frees ~60 KB RAM)

> Onboard LED off at boot

Everything restores automatically the moment a client reconnects. A power-cycle (ignition off/on) will also bring WiFi back if required.

## PCB Design
The PCB has been designed in EasyEDA and available in the 'PCB' folder.

## PCB Principles
The board is based around an ESP32 with an external CAN chip (SN65HVD230) with an optional termination resistor (if there are no other CAN devices on the network). 

The outputs are controlled via. a ULN2003A (a 200mA 7-channel Darlington Array) which is socketed so that if damaged can be swapped out.  This array is used to trigger each of the outputs - including the high-side 5A MOSFET (for reverse light, typically).

GPS is captured via. a Neo6M module and via. a Software Serial connection.  It is supplied with an external aerial - although there may be a requirement to add a cabled external aerial.  The connector is a standard SMA connector.  

The aerial that can be found on Google with a search "sma gps antenna".

![Board Top](/Images/BoardTop.png)
